import 'package:flutter/material.dart';

class ChampaignLayout extends StatelessWidget{
  const ChampaignLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is champaign layout.'),
      ),
    );
  }

}